# تصحيح تجربة الدخول المحلي للمدرسة المستقلة

تم تنفيذ تصحيح إضافي على عقد دخول المدارس المستقلة بحيث يمكن اختبار الدخول محليًا قبل الرفع على GitHub.

## ما تم تصحيحه

1. قفل مطابقة المستخدم المحلي داخل نفس المدرسة فقط:
   - `schoolId`
   - `school_id`
   - `schoolCode`
   - `school_code`
   - `registrationCode`
   - `registration_code`
   - أو بريد المدير المرتبط بنفس المدرسة.

2. دعم دخول مدير المدرسة التجريبية محليًا حتى إذا كان المدير محفوظًا داخل سجل المدرسة فقط وليس داخل جدول المستخدمين المحلي.

3. عند إنشاء مدرسة محليًا من لوحة إدارة المدارس، يتم الآن إنشاء:
   - سجل المدرسة في `smart_school_schools`.
   - حساب مدير محلي في `smart_school_users`.
   - كلمة مرور افتراضية `123456` إذا تُرك حقل كلمة المرور فارغًا.

4. منع الخلط بين المدارس عند فتح أكثر من رابط مدرسة مستقلة على نفس المتصفح.

## الملفات المعدلة

- `school-login.html`
- `index.html`

## طريقة التجربة المحلية

1. افتح `index.html` محليًا.
2. ادخل إلى إدارة المدارس.
3. أنشئ مدرسة تجريبية أو استخدم المدرسة المحفوظة مسبقًا.
4. انسخ رابط الدخول الخاص بالمدرسة.
5. افتح الرابط محليًا.
6. يجب أن يظهر اسم المدرسة في واجهة الدخول.
7. سجّل الدخول ببريد مدير المدرسة وكلمة المرور المحفوظة، أو `123456` إذا كانت المدرسة أنشئت محليًا بدون كلمة مرور.

## نتيجة الفحص

تم فحص سكربتات `school-login.html` و `index.html` نحويًا عبر Node.js بدون أخطاء.
