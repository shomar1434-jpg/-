# RL73 — تثبيت تحميل التصحيحات وإظهار الإداريين المرتبطين بالمدرسة

المشكلة التي بقيت بعد RL71:
- رسالة SUPABASE_ANON_KEY is not defined بقيت كما هي.
- قائمة الإداريين بقيت فارغة.

السبب المرجح المؤكد بنيوياً:
admin_employee_management.html كان يستدعي supabase-bridge.js بدون أي version query،
ولذلك يمكن للمتصفح/GitHub Pages إعادة النسخة القديمة المخزنة في الكاش حتى بعد رفع RL71.

RL73:
1. يثبت supabase-bridge.js المصحح من RL71.
2. يثبت platform-cloud-session.js المصحح من RL70.
3. يجعل صفحة الإدارة تستدعيهما بعناوين version جديدة لإجبار المتصفح على تحميل النسخ المصححة.
4. يصحح وصف مصدر القائمة ليصبح الارتباط بالمدرسة، لا رابط التسجيل فقط.

ملاحظة:
تم بالفعل نشر platform-directory v5 في Supabase لاستعادة الحسابات الإدارية التاريخية
المرتبطة بنفس school_id، مع منع أي خلط بين المدارس.

ارفع الملفات الثلاثة معاً.
