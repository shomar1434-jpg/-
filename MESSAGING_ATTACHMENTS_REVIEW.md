# مراجعة نظام المراسلات الداخلي — المرفقات

## سبب الخلل
كان المرفق يرفع باسم المستخدم المرسل ويظل ملفًا مملوكًا له. شاشة المستلم كانت تحاول فتحه مباشرة عن طريق CloudFileEngine، بينما محرك الملفات يمنع المستخدم العادي من فتح ملف مملوك لمستخدم آخر. لذلك تظهر الرسالة للمستلم بينما يفشل فتح المرفق.

## التصحيح
- أضيفت عملية خادمية `attachment-url` إلى `private-school-messages`.
- لا يصدر الرابط إلا بعد التحقق من:
  1. المدرسة نفسها.
  2. أن المستخدم هو المرسل أو أحد مستلمي الرسالة.
  3. أن الملف مرتبط فعليًا بهذه الرسالة.
  4. أن الملف ما زال فعالًا.
- الرابط Signed URL مؤقت لمدة 10 دقائق.
- أثناء الإرسال يتم التحقق من جميع المرفقات وعدم السماح برسالة تدّعي وجود مرفقات لم يتم حفظها.
- تُنشأ روابط `platform_file_links` للمرفقات الجديدة.
- الواجهة تتحقق من `attachmentCount` بعد الإرسال.
- إعادة التوجيه أصبحت تحمل مرفقات الرسالة الأصلية بدل فقدها.

## مراجعة قواعد المراسلات
لم تتغير قواعد المستلمين:
- المالك -> المدير فقط.
- المدير -> جميع المستخدمين المسموحين، والمالك مباشرة.
- الوكيل -> المدير وبقية مستخدمي المدرسة، باستثناء المالك.
- بقية المستخدمين -> الوكيل فقط.
- الإرسال الجماعي للمدير والوكيل فقط.
- العزل بالمدرسة مفروض في كل قراءة وإرسال وفتح مرفق.

## اختبارات
- JavaScript syntax check لـ internal_messages_app.js: ناجح.
- Edge Function `private-school-messages` تم نشرها على Supabase كالإصدار 6 وحالتها ACTIVE.
- تمت مراجعة جداول internal_messages / internal_message_recipients / internal_message_attachments / platform_files / platform_file_links.
