# تنفيذ مزامنة غياب الطلاب من نظام نور

## نطاق التعديل

- التعديل محصور في صفحة «الغياب والمواظبة» داخل إجراءات شؤون الطلاب.
- زر «تسجيل حركة» بقي مخصصًا للتأخر الصباحي والاستئذان.
- زر «مزامنة الغياب من نور» يتصل بتبويب نور المفتوح عبر إضافة Chrome.
- أضيف زر منفصل «استيراد ملف غياب» كخيار احتياطي.
- تعرض المنصة معاينة قبل الحفظ: جاهز، مكرر، غير مطابق، وتاريخ مختلف.
- الحفظ يتم عبر وظيفة Supabase تتحقق من جلسة المنصة والمدرسة ودور وكيل/وكيلة شؤون الطلاب.
- لا تعتمد الوظيفة على `school_id` أو هوية المستخدم المرسلة من الإضافة.
- يمنع القيد الفريد تكرار غياب الطالب/الطالبة في المدرسة واليوم نفسيهما.
- الجدول محمي بـRLS ومسحوبة منه صلاحيات `anon` و`authenticated`؛ الوصول عبر الوظيفة فقط.

## ترتيب الدمج والنشر

1. ادمج الملفات بمساراتها كما هي.
2. ادفع التغييرات إلى فرع `main`؛ الـworkflow يطبق ترحيلات قاعدة البيانات ثم ينشر الوظيفة.
3. ثبّت مجلد `browser-extension-noor-sync` كإضافة غير مضغوطة في Chrome، أو حزّمه للنشر المؤسسي.
4. حدّث صفحة المنصة بعد تثبيت الإضافة.
5. افتح كشف الغياب اليومي في نور، ثم نفّذ المزامنة من صفحة الغياب والمواظبة.

## التحقق المنفذ

- فحص صياغة جميع ملفات JavaScript في الإضافة: ناجح.
- فحص 16 كتلة JavaScript داخل صفحة إجراءات شؤون الطلاب: بلا أخطاء صياغة.
- فحص بنية `manifest.json`: ناجح.
- 12 تحققًا ساكنًا لمسار الاتصال، وعزل المدرسة، وRLS، ومنع التكرار: ناجحة.

> لا يمكن اختبار استخراج نور اختبارًا نهائيًا دون جلسة نور حقيقية وصفحة كشف فعلية؛ عند اختلاف عناوين أعمدة نور سيعرض النظام رسالة واضحة ولن يحفظ بيانات جزئية.
