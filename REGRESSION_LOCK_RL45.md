# RL45 — إصلاح القراءة الفعلية لمكتبة القسم وأرشيف مجالات الأداء مع عزل صارم

## سبب الخلل
RL44 أبقى العزل لكنه استخدم قارئًا عامًا لأرشيف الأداء لا يطابق مفاتيح التخزين الحقيقية، كما أن فتح مكتبة القسم لم يكن يضمن الاستفادة من مسار الاسترداد التاريخي الذي تستخدمه المكتبة الأصلية.

## التصحيح
- مكتبة القسم تستمر عبر `EvidenceLibrarySource.open('impact', ...)` حتى تستخدم نفس آلية الاسترداد/القراءة الخاصة بمكتبة الدور.
- أضيف حارس إضافي في قياس الأثر يرفض أي عنصر لا يطابق المدرسة + المستخدم + الدور الحالي.
- أرشيف مجالات الأداء أصبح يقرأ فهرس الدور الحقيقي ثم مفاتيح التقارير الحقيقية، وكلها `scope=user`.
- مثال المعلم:
  - `moduleKey=teacher`
  - `teacher_perf_index_v1`
  - `teacher_perf_report_v1_*`
- أضيفت خرائط مقابلة للأدوار الأخرى دون فتح أرشيف دور مختلف.
- لا يوجد fallback إلى localStorage ولا قراءة school-wide لأرشيف الأداء.

## ملفات التعديل
- `impact_assessment.html` فقط.

## ما لم يتغير
لا تعديل على `evidence-library-source.js` أو `platform-state` أو `platform-files` أو Backend أو ملفات العزل أو بقية المنصة.
