# RL33 — اختبار العزل بعد الرفع

نفّذ الاختبار بعد رفع ملفات الواجهة ونشر Edge Functions، وقبل تطبيق قفل قاعدة البيانات النهائي. بعد رفع RL33 سجّل الدخول من جديد للحسابات التجريبية؛ التبويب الجديد لا يرث جلسة تبويب آخر:

1. افتح المدرسة (أ) بحساب معلم في تبويب، ونفس المدرسة بحساب إداري في تبويب ثانٍ من نفس المتصفح.
2. تنقّل في كلا التبويبين ثم حدّثهما: يجب أن يبقى كل تبويب على user_id + role الخاصين به ولا تتبدل الواجهة.
3. من حساب المعلم افتح «مكتبة القسم»: يجب أن تظهر section_library_teacher الخاصة بنفس user_id فقط.
4. من حساب الإداري افتح «مكتبة القسم»: يجب أن تظهر section_library_administrative_employee الخاصة بنفس user_id فقط.
5. افتح «أرشيف مجالات تقارير الأداء» من قياس الأثر: يجب ألا يظهر إلا أرشيف المستخدم الحالي؛ اختبر مستخدمين لهما نفس الدور أيضًا.
6. اختبر مدرسة (ب) بالتوازي: لا يجب أن يظهر أي ملف/حالة/عضوية من المدرسة (أ).
7. جرّب فتح رابط صفحة دور مختلف يدويًا (مثال teacher -> manager.html): يجب رفض المسار وإعادة المستخدم لدوره.
8. اختبر تبديل المدرسة/الدور من مبدل العضويات: التبديل يجب أن يتم عبر جلسة خادمية صريحة، ولا يغيّر تبويبًا آخر.
9. المدير يرى بنك قياس الأثر المؤسسي للمدرسة فقط؛ المستخدم العادي يرى قياساته واستباناته فقط.
10. أنشئ استبانة عامة وجرّب الرابط/QR دون تسجيل دخول؛ يجب أن تظهر الاستبانة فقط دون school_id أو user_id.

بعد نجاح هذه النقاط طبّق `supabase/migrations/20260905_rl33_independent_school_isolation_lock.sql` ثم كرر البنود 1–10. هذا القفل يمنع الوصول المباشر من المتصفح إلى الجداول الحساسة ويُبقي الوصول عبر المحركات الخادمية فقط.
