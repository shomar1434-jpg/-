# التصحيح الأمني النهائي — مدير المدرسة + مدير النظام

## البنية المرجعية
- بني التصحيح على V5.4 الحالية، وليس على حزمة أقدم.
- لم تُعد كتابة منطق دخول الأقسام الأصلية.
- بقي محرك تقارير الأداء V5.4 دون تعديل.

## مسار مدير المدرسة
1. `manager.html` يتحقق من جلسة PlatformCloudSession ودور المدير.
2. مركز المعلومات يستدعي `ensure()` ثم `verifyAccess(['manager'])`.
3. `school-information` يأخذ `school_id` من `platform_sessions` على الخادم فقط.
4. أي `schoolId` يرسله مدير المدرسة في الرابط أو body لا يستخدم كمصدر صلاحية.
5. جميع عمليات الطلاب والموظفين مقيدة بـ `school_id` الموجود في الجلسة.

## مسار مدير النظام
1. يتم التحقق من Supabase Auth session.
2. يتم التحقق من جدول `system_admins` عبر وظيفة `system-admin` قبل السماح بالسياق الإداري.
3. مركز المعلومات يعرض اختيار المدرسة لمدير النظام فقط إذا لم تكن مدرسة مستهدفة موثقة موجودة.
4. قائمة المدارس تأتي من `system-admin?action=list_schools` وليس من قراءة عامة للجدول.
5. خدمة `school-information` تعيد التحقق من JWT ومن `system_admins` ثم تتحقق من وجود المدرسة المستهدفة.
6. اختيار المدرسة لا ينشئ جلسة مدير مدرسة ولا يغير هوية مدير النظام.
7. العودة من المركز تعيد مدير النظام إلى `index.html?systemAdminReturn=1`.

## منع اختلاط البيانات
- `school-information-source.js` لا يدمج localStorage عند سياق مدير النظام.
- عند مدير النظام يستخدم المصدر نفس المدرسة المستهدفة الموثقة ونفس Edge Function.
- عند مدير المدرسة لا يزال مصدر المدرسة هو PlatformCloudSession فقط.

## الفحص الساكن
- `manager.html`: 27 كتلة JavaScript تنفيذية، 0 أخطاء Syntax.
- `school_information_center.html`: 4 كتل JavaScript تنفيذية، 0 أخطاء Syntax.
- `platform-cloud-session.js`: اجتاز `node --check`.
- `school-information-source.js`: اجتاز `node --check`.
- Edge Function `school-information` منشورة في Supabase بالإصدار 2 وحالتها ACTIVE.

## اختبارات القبول المطلوبة في النسخة المنشورة
- مدير مدرسة A يفتح المركز: تظهر مدرسة A فقط.
- تعديل URL وإضافة schoolId لمدرسة B من حساب مدير A: يجب أن يبقى الخادم على مدرسة A.
- فتح المركز من مدير النظام: يتحقق الحساب ثم يسمح بتحديد مدرسة.
- مدير النظام يختار مدرسة A: تظهر بيانات A فقط.
- تبديل مدير النظام إلى مدرسة B: تظهر بيانات B فقط دون تحويله إلى جلسة مدير مدرسة.
- مستخدم غير مصرح: يمنع من manager.html ومن مركز المعلومات.
