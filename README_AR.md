RL197 — مربعات اختيار ظاهرة أمام سجلات المدير والوكيل عند نطاق «سجل محدد من المجموعة». القائمة تبقى مفتوحة لاختيار عدة سجلات، وكلها ترتبط بالتكليف الواحد عبر adapter السحابي. ارفع central_task_center.html وcentral-task-cloud-adapter.js معًا إلى جذر المستودع؛ تحديث رقم نسخة السكربت في الصفحة يمنع استخدام نسخة المتصفح القديمة. لا Edge Functions ولا تغيير بيانات قديمة. تحقق من ظهور المربعات في المتصفح بعد الرفع.
