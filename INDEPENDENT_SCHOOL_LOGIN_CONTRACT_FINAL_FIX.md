# تصحيح عقد رابط دخول المدرسة المستقلة

تم إصلاح الخلل البنيوي في رابط دخول المدرسة المستقلة:

- رابط الدخول الصادر من إدارة المدارس أصبح يحمل: `schoolId`, `id`, `schoolCode`, `school`, `school_name`, `schoolName`, `manager_email`, `email`.
- صفحة الدخول لم تعد تعتمد على `localStorage` لتحديد المدرسة عند فتح الرابط حتى لا يحدث تداخل بين مدارس مختلفة على نفس المتصفح.
- عند فتح الرابط يظهر اسم المدرسة مباشرة إذا كان موجودًا في الرابط، أو يتم جلبه من Supabase عبر `id / school_code / registration_code / manager_email`.
- دخول المدير الموثق في Supabase Auth أصبح يربط الحساب بمدرسته حتى لو لم يكن `school_id` محفوظًا في صف `users`، عبر `schools.manager_email`.
- أضيفت دوال إدارة المدارس في `index.html` لضمان أن زر "رابط الدخول" ينتج رابطًا خاصًا بكل مدرسة وليس رابطًا عامًا.

الملفات المعدلة:
- `school-login.html`
- `index.html`
