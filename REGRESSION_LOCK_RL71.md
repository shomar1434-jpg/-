# RL71 — إصلاح الخطأ الظاهر SUPABASE_ANON_KEY is not defined

السبب المؤكد من الرسالة التي ظهرت في المتصفح:
supabase-bridge.js يعرّف المفتاح في أعلى الملف باسم:
SUPABASE_KEY

لكن وظائف RL33 الجديدة تستخدم متغيراً مختلفاً غير معرّف:
SUPABASE_ANON_KEY

الموضعان المتأثران:
1) systemAdminCall()
2) platformDirectoryCall()

والثاني هو المسار المستخدم مباشرة في:
- list-admin-employees لعرض الإداريين المرتبطين بالمدرسة.
- school-registration-context وإنشاء روابط تسجيل/دخول الموظف الإداري.
- عمليات التفعيل والتعطيل المرتبطة بدليل المدرسة.

لذلك خطأ واحد يفسر العرضين اللذين ظهرَا معاً:
- رسالة SUPABASE_ANON_KEY is not defined عند الضغط على الروابط.
- عدم ظهور الموظفين الإداريين.

التصحيح:
استبدال المرجعين غير المعرّفين بـ SUPABASE_KEY المعرّف فعلياً في نفس الملف.

النطاق:
supabase-bridge.js فقط.
لا تعديل على نظام العزل أو Supabase أو بيانات المدارس أو الموظفين.
