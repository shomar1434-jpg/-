# التصحيح الجذري لرابط المدارس المستقلة

تم تعديل صفحة `school-login.html` لتمنع الرجوع إلى صفحة عامة عند وجود `schoolId/sid/schoolCode` في الرابط.

## نقاط التصحيح
- تشغيل تهيئة صفحة الدخول فورًا سواء حدث `DOMContentLoaded` أو كانت الصفحة جاهزة مسبقًا.
- قراءة عقد المدرسة المحلي المحفوظ عند نسخ الرابط من إدارة المدارس قبل طلب Supabase.
- جلب المدرسة من Supabase عبر `id / school_code / registration_code`.
- عند فشل مطابقة الرابط مع مدرسة، لا يتم السماح بالدخول كرابط عام.
- لا يتم اعتماد اسم المدرسة أو بريد المدير من الرابط كصلاحية تشغيلية.
- الدخول النهائي مشروط بتطابق المستخدم مع نفس المدرسة عبر `users.school_id` أو `school_members` أو `schools.manager_email`.

## قاعدة الأمان
الرابط العام غير مقبول إذا احتوى على معرف مدرسة ولم يطابق سجل مدرسة فعلي.
