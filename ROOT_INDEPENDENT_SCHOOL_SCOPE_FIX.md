# التصحيح الجذري لنطاق دخول المدرسة المستقلة

تم إلغاء الاعتماد على اسم المدرسة أو بريد المدير القادم من الرابط كحقيقة تشغيلية.

## عقد الرابط الجديد
- الرابط يحمل فقط معرف المدرسة/كودها: `sid`, `schoolId`, `scode`, `schoolCode`.
- لا يتم استخدام `sname` أو `memail` لتكوين الجلسة أو منح الصلاحية.
- يتم جلب اسم المدرسة وبريد المدير من سجل المدرسة في Supabase أو التخزين المحلي عند التجربة المحلية.

## قواعد الدخول
لا يسمح بالدخول إلا إذا تحقق أحد الشروط داخل نفس المدرسة:
1. `users.school_id` يطابق معرف المدرسة في الرابط.
2. وجود عضوية في `school_members` لنفس المدرسة.
3. بريد المستخدم يطابق حقول مدير المدرسة داخل سجل `schools` لنفس المدرسة فقط.

## دعم المجمعات التعليمية
مدير أكثر من مدرسة لا يدخل إلا من رابط المدرسة المطلوبة، ثم يتم تحديد `school_id` الخاص بهذه المدرسة داخل الجلسة.

## حماية منع التداخل
إذا لم يتم العثور على المدرسة من `sid/scode` في قاعدة البيانات أو التخزين المحلي، تتوقف صفحة الدخول ولا تعرض بيانات شكلية من الرابط.
