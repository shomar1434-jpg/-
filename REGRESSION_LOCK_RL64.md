# RL64 — إزالة ومضة واجهة الطباعة أثناء حفظ سجلات التقويم الذاتي

السبب:
مسار writeArchiveRecord كان يغيّر `isPrinting=true` في تطبيق Vue المرئي قبل إنشاء نسخة الأرشيف.
هذا أعاد ترتيب الواجهة أمام المستخدم للحظات، فظهرت واجهة الطباعة كوميض عند الضغط على الحفظ.

التصحيح:
- أزيل تغيير `isPrinting` من مسار الحفظ والأرشفة.
- أزيل تغيير `isPrinting` من مسار معاينة الأرشيف.
- بناء HTML يتم من clone ثابت دون تعديل DOM المرئي.
- إنشاء Canvas/PDF يستمر داخل iframe معزول خارج الشاشة.
- iframe لا يشارك في تخطيط الصفحة ولا يظهر للمستخدم.
- أزرار الطباعة الصريحة للمستخدم لم تتغير؛ التعديل خاص بالحفظ/الأرشفة فقط.

Regression Lock:
- مبني على RL63.
- يحافظ على استبدال PDF الأبيض بمعرف جديد والتحقق من V5.
- يحافظ على RL62: النص الثابت ومنع مربعات الإدخال.
- manager.html لم يتغير وظيفياً عن RL63.
- لا تعديل على teacher.html أو impact_assessment.html أو RL61.
- الملفات المسلّمة: self_evaluation_records.html و manager.html (manager مطابق لـ RL63) + تقرير القفل.
