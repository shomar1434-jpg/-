# RL198 — تصحيح التوجيه القانوني لسجلات مركز التكليفات

## النطاق

- تصحيح فتح السجل المخالف للاسم الظاهر في بطاقة التكليف.
- دعم التكليفات الحالية والقديمة دون حذفها أو تعديل أعمالها السابقة.
- منع اعتماد مسار `route_url` تاريخي عند توفر تعريف حديث في القاموس الموحد.
- منع استمرار نسخة قديمة من قاموس السجلات في ذاكرة المتصفح.

## السبب الجذري

كان فتح السجل يقدّم المسار المخزن داخل التكليف على المسار الحالي في `PlatformRecordCatalog`. لذلك ظل بعض التكليفات القديمة يفتح سجلًا آخر بعد تصحيح القاموس. كما كانت الصفحات تستدعي `platform-record-catalog.js` دون رقم إصدار، مما يسمح ببقاء نسخة متصفح قديمة.

## التصحيح

1. مطابقة السجل بالهوية الثلاثية: `moduleKey + recordType + recordId`.
2. عند تعارض الاسم الظاهر مع معرّف تاريخي، يستخدم الاسم الفريد داخل قسم المدير أو الوكيل لاستعادة التعريف القانوني.
3. تقديم `d.routeUrl` الحالي على `row.route_url` التاريخي.
4. تثبيت معامل `record` في روابط `manager_records.html` و`wakil-records.html` بالمعرّف القانوني.
5. منع أي مسار يخرج عن أصل المنصة.
6. ترقية كاش القاموس إلى `platformCanonicalRecordCatalogV2_RL198` وعدم قراءة الكاش القديم.
7. إضافة رقم إصدار موحد لاستدعاء القاموس في جميع الصفحات المرتبطة.

## السلامة

- لا يوجد حذف أو تعديل لبيانات التكليفات السابقة.
- لا يوجد تعديل لقاعدة البيانات أو Edge Functions أو سياسات RLS.
- لا يحتاج ملف النشر المركزي إلى تغيير في هذا التصحيح لأنه تصحيح واجهة ثابتة فقط.

## التحقق

- نجح فحص صياغة JavaScript داخل مركز التكليفات.
- نجح فحص صياغة `platform-record-catalog.js`.
- جرى فحص 106 صفحات HTML ولم يبق استدعاء غير مقيّد بإصدار للقاموس.
- تحقق 19 سجل مدير و50 سجل وكيل، وجميع معرفاتها فريدة.
- تحقق أن المسار القانوني مقدم على المسار التاريخي وأن معامل `record` يعاد تثبيته قبل الانتقال.
