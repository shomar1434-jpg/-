# فحص تصحيح حفظ تقارير الأداء — الوكيل فقط

- الملف المعدل: `agent.html` فقط.
- لم يتم تعديل `wakil-records.html` أو أي ملف لدور آخر.
- التصحيح محصور في مسار زر «حفظ في الأرشيف» لتقارير الأداء.
- يثبت المجال المختار في `category` و`archiveFolderId` بالقيمة نفسها.
- ينتظر التحقق السحابي الفعلي، ثم يعيد قراءة فهرس الأرشيف، ثم يتأكد من وجود نفس التقرير داخل مجلد المجال المختار.
- لا تعرض رسالة نجاح نهائية قبل اكتمال هذا التحقق.
- رسالة النجاح تعرض اسم المجلد صراحة: «تم الحفظ في أرشيف تقارير الأداء الوظيفي / [اسم المجال]».
- عند فشل التحقق تظهر رسالة فشل ولا يتم الادعاء بنجاح الحفظ.
- بقية `agent.html` بقي مطابقًا حرفيًا للنسخة المدخلة؛ أضيفت طبقة التصحيح قبل `</body>` النهائي فقط.
- فحص JavaScript لطبقة التصحيح الجديدة عبر Node `--check`: PASS.
