# تصحيح اختيار المراحل وسرعة الدخول والعودة

يجب رفع الملفات الأربعة معًا إلى جذر المنصة مع الاحتفاظ بأسمائها:

1. `interactive_operational_plan.html`
2. `manager.html`
3. `school-login.html`
4. `platform-cloud-session.js`

## سبب عدم ظهور الاختيار المتعدد

كان رابط بطاقة الخطة في `manager.html` يفتح اسم الملف دون إصدار، مما يسمح للمتصفح أو GitHub Pages بإعادة نسخة HTML القديمة المخزنة. أصبح الرابط يستخدم إصدارًا جديدًا ثابتًا، وبذلك تُحمّل واجهة المراحل المتعددة فعليًا.

## تصحيح البطء

كان محرك الجلسة يستدعي آلية الاحتفاظ بنتيجة التحقق الحديثة، لكن مفتاح هذه الآلية كان غير معرّف؛ لذلك كان الفشل يحدث بصمت ويُعاد فحص العضوية خادميًا عند التنقل والعودة. تم تعريف المفتاح وربط نتيجة فتح الجلسة الموثقة به.

- أول دخول يبقى محميًا بالتحقق الخادمي من المدرسة والعضوية والدور.
- لا يعاد التحقق نفسه أثناء التنقل الحديث إذا تطابقت المدرسة والمستخدم والدور وكانت الجلسة صالحة.
- لا يوجد اعتماد على بيانات مدرسة عامة أو نقل جلسة بين مدرستين.

## العودة من الخطة

زر العودة يستخدم سجل المتصفح عند القدوم من `manager.html` ليستعيد واجهة المدير مباشرة، ويستخدم `location.replace` كمسار احتياطي فقط.

## الاختبار

1. ارفع الملفات الأربعة معًا.
2. نفذ تحديثًا قسريًا للمتصفح مرة واحدة بعد الرفع.
3. ادخل إلى مدرسة مستقلة، ثم افتح بطاقة الخطة التشغيلية.
4. تحقق من ظهور خمس خانات اختيار للمراحل وإمكانية اختيار أكثر من مرحلة.
5. ارجع إلى واجهة المدير وتحقق من عدم إعادة دورة الدخول أو انتظار فحص العضوية كاملًا.
