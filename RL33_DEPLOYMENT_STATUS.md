# RL33 — حالة النشر وخطوات الإغلاق النهائي

تم تحديث البيئة السحابية أثناء التطوير للوظائف التالية:
- platform-session: منطق الجلسة/الدور الصريح ومنع التبديل الصامت.
- platform-directory: دليل الهوية والعضوية والتسجيل وربط الموظف الإداري بالمشرف المحدد.
- impact-measurement: عزل قياس الأثر حسب المدرسة + المستخدم، وبنك المدير بصلاحية مؤسسية.

ويجب أن ينشر رفع هذه الحزمة عبر GitHub Workflow النسختين الموجودتين داخل الحزمة من:
- platform-state: عزل الحالة الخاصة وأرشيف مجالات تقارير الأداء حسب user_id + role مع استرداد السجلات القديمة الآمن فقط إذا updated_by يطابق المستخدم.
- platform-files: عزل مكتبات الأقسام والملفات الخاصة حسب school_id + user_id + role ومنع طلب مكتبة دور آخر من الخادم.

## مهم: قفل قاعدة البيانات النهائي
الملف:
`supabase/migrations/20260905_rl33_independent_school_isolation_lock.sql`

تم فحصه على مخطط قاعدة البيانات الحالية داخل transaction مع ROLLBACK ونجح تركيبيًا. لا يطبق قبل رفع واجهات RL33 ونشر Edge Functions لأن الواجهة القديمة تحتوي مسارات وصول مباشر ستتوقف بعد القفل.

الترتيب:
1. رفع ملفات RL33 إلى المستودع.
2. التأكد من نجاح GitHub Action ونشر platform-state وplatform-files وبقية الدوال المحددة في workflow.
3. تسجيل الدخول من جديد بحسابات الاختبار وتنفيذ RL33_SMOKE_TEST.md.
4. بعد نجاح الاختبار يطبق ملف migration النهائي.
5. إعادة اختبار السيناريوهات نفسها بعد القفل.

ملاحظة: RL33 لا يسمح لتبويب جديد أن يرث جلسة سحابية مصادقًا عليها من localStorage؛ لذلك يلزم تسجيل دخول مستقل للتبويب/الحساب عند الحاجة.
