# RL46 — متصفح مجلدات سحابي لمصادر شواهد قياس الأثر

## ما تم
- «مكتبة القسم»: يتم أولاً تشغيل `recover-section-library` ثم قراءة المكتبة الحالية والملفات التاريخية الآمنة، مع فلترة إلزامية `school_id + owner_user_id + role`.
- يعرض نموذج قياس الأثر مجلدات مكتبة المستخدم أولاً، ثم يفتح المجلد، ثم يختار المستخدم الملف.
- «أرشيف مجالات تقارير الأداء»: يعرض مجلدات/مجالات الأرشيف أولاً ثم التقارير داخل المجال.
- اختيار الملف يربط مرجع الملف/التقرير الموجود سحابيًا فقط؛ لا يعيد رفعه ولا ينشئ نسخة جديدة.
- يمنع إضافة نفس الشاهد مرتين داخل نموذج قياس الأثر.
- ملفات المكتبة التاريخية تُقرأ فقط إذا كانت مملوكة لنفس المستخدم ونفس المدرسة، ومع فصل صريح للصحة والنشاط عند recordType القديم المشترك.

## العزل
- لا توجد قراءة school-wide لمكتبة القسم.
- لا توجد قراءة لأرشيف مستخدم آخر.
- تطابق `owner_user_id + school_id + role` إلزامي للمصادر الشخصية.
- لا يوجد fallback إلى localStorage.
- لم يتم تعديل Backend أو Supabase أو أي ملف خارج `impact_assessment.html`.
