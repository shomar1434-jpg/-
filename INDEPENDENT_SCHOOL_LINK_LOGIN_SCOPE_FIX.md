# تصحيح دخول المدارس المستقلة من رابط الدخول

## المشكلة
عند نسخ رابط الدخول من لوحة إدارة المدارس وفتحه في المتصفح:
- لا تظهر بيانات المدرسة على واجهة الدخول.
- حساب مدير/ة المدرسة لا يقبل الدخول رغم أنه موجود وفعال في Supabase.

## السبب الجذري
كان رابط الدخول في بعض الحالات يمرر معرفًا مثل `SCH-QG1HYA` داخل `schoolId`، بينما محرك الدخول كان يتعامل مع `schoolId` على أنه `id` داخلي للمدرسة فقط.
لذلك كانت صفحة الدخول تفشل في تحديد المدرسة، ثم تفشل مطابقة حساب المدير داخل نطاق المدرسة.

## ما تم إصلاحه
- دعم قراءة المدرسة من الرابط سواء أتى كـ:
  - `schoolId`
  - `school_id`
  - `id`
  - `school`
  - `schoolCode`
  - `school_code`
  - `registrationCode`
  - `registration_code`
- البحث في Supabase عن المدرسة عبر:
  - `id`
  - `school_code`
  - `registration_code`
- عند إيجاد المدرسة يتم تحويل المرجع إلى `id` الحقيقي قبل التحقق من حساب المدير.
- دعم حسابات المدير المرتبطة عبر:
  - جدول `users`
  - جدول `school_members`
  - حقل `schools.manager_email`
- تحديث عنوان واجهة الدخول ليعرض اسم المدرسة بعد التعرف عليها.

## الملفات المعدلة
- `school-login.html` فقط.

## التحقق البرمجي
تم فحص سكربتات `school-login.html` نحويًا عبر Node وكانت النتيجة: Passed.
