# RL44 — عزل مصادر شواهد قياس الأثر بحسب المدرسة + المستخدم + الدور

## السياسة المنفذة
- كل المستخدمين: «مكتبة القسم» من `EvidenceLibrarySource` المعزول بحسب `school_id + user_id + role`، و«أرشيف مجالات تقارير الأداء» من `pull-performance-archive` مع تحقق صريح أن `ownerKey === currentUserId` وأن الدور يطابق جلسة الصفحة.
- مدير المدرسة: يضاف «أرشيف السجلات» المؤسسي للمدير و«أرشيف التقويم الذاتي» للمدرسة.
- الوكيل: يضاف «أرشيف السجلات» الخاص بملفات الوكيل فقط من `agent_records_archive` مع `ownershipScope=user` وفحص `owner_user_id + school_id`.
- بقية الأدوار لا ترى مصادر المدير أو الوكيل.
- تم تعطيل الإدخال اليدوي داخل منتقي المصادر السحابية حتى لا يُلصق رابط من مصدر آخر ويُوسم خطأً على أنه من الأرشيف الحالي.

## حماية من الرجوع
- لا يوجد فحص localStorage لمكتبات أو أرشيفات أدوار أخرى.
- لم يتم تعديل `evidence-library-source.js` أو `platform-state` أو `platform-files` أو أي Backend.
- لم يتم تعديل تحليل نافس أو القدرات والتحصيلي أو واجهات المدير/الوكيل.
- التعديل في ملف واحد فقط: `impact_assessment.html`.

## ملاحظة بنيوية
أرشيف التقويم الذاتي وأرشيف سجلات المدير مصادر مؤسسية تخص المدرسة ويظهران للمدير فقط، بينما مكتبة القسم وأرشيف مجالات الأداء وأرشيف ملفات الوكيل تُحصر بالمستخدم الحالي.
