# RL199 — تصحيح تعطل دخول المدارس المستقلة

## السبب الجذري

كانت صفحة الدخول تنفذ مصادقتين متتاليتين لنفس المحاولة:

1. قراءة المستخدم والمدرسة مباشرة من Supabase داخل المتصفح.
2. إعادة المصادقة وإنشاء جلسة موقعة عبر `platform-session`.

كانت القراءة الأولى محاطة بمهلة 15 ثانية. عند بطء الشبكة أو كثرة استعلامات الحساب كانت الصفحة تعرض «انتهت مهلة الاتصال أثناء قراءة حساب المدرسة» وتتوقف قبل استدعاء بوابة الجلسة المركزية.

كما كانت وظيفة `platform-session` تعيد تحميل حتى 5000 مستخدم مرتين داخل بعض مسارات التوافق التاريخية.

## التصحيح

- اعتماد `platform-session` بوصفها مسار الدخول المركزي الوحيد.
- إزالة القراءة المباشرة المكررة من المسار الحرج لواجهة الدخول.
- تثبيت المدرسة والعضوية والدور من الجلسة الموقعة فقط.
- إضافة مهلة شبكية حقيقية بـ`AbortController` مقدارها 25 ثانية.
- منع النقرات المتكررة من إنشاء عدة جلسات متزامنة.
- منح بوابة الجلسة 30 ثانية في واجهة الدخول، مع إلغاء الطلب نفسه عند انتهاء مهلة الشبكة.
- تقليل استعلام المستخدمين للبريد إلى استعلام محدد، مع إبقاء التوافق التاريخي احتياطًا.
- إعادة استخدام نتيجة فحص الهوية بدل تحميل جدول المستخدمين للمرة الثانية.
- إضافة `probe` آمن إلى وظيفة `platform-session`.
- إضافة نشر صريح وفحص وصول لـ`platform-session` داخل workflow المركزي، مع الحفاظ على نشر جميع الوظائف والفحص السابق لمساحة المستندات.
- تحديث إصدار `platform-cloud-session.js` في صفحة الدخول لمنع استمرار النسخة القديمة في المتصفح.

## السلامة والعزل

- لا يسمح التصحيح بالدخول المحلي دون جلسة خادمية موقعة.
- رابط المدرسة يظل مثبتًا على `schoolId` المحدد ولا يسمح بالنقل إلى مدرسة أخرى.
- لا تُستخدم بيانات `user_metadata` لتحديد الصلاحيات.
- لم تُحذف أو تُعدّل حسابات المدارس أو كلمات المرور.
- لم تُضعف سياسات RLS أو صلاحيات Supabase.

## التحقق

- نجح فحص صياغة JavaScript لصفحة الدخول و`platform-cloud-session.js`.
- تحقق غياب مسار «قراءة حساب المدرسة» المكرر من عملية الدخول.
- تحقق وجود مهلة الجلسة الجديدة ومنع الطلبات المتزامنة.
- تحقق وجود مسار النشر والفحص الصريح لوظيفة `platform-session` في workflow المركزي.
- يلزم بعد رفع الحزمة تشغيل GitHub Actions والتأكد من نجاح `Verify platform-session is reachable`، ثم تجربة رابط مدرستين مستقلتين على الأقل.
