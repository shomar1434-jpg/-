# مراجعة إصلاح قسم الوكيل — 2026-08-31

تم تطبيق الإصلاح على النسخة المرفوعة في هذه الجولة فقط.

## الإصلاحات
- منع الومضة الأولى عبر first-paint guard حتى DOMContentLoaded وإطارَي رسم.
- تعطيل sectionAppear على واجهات الوكيل لمنع إعادة opacity من الصفر عند التنقل الداخلي.
- جعل حقن زر «من مكتبة القسم» idempotent وربطه بمعرف فريد لحقل الملف، مع تنظيف الأزرار اليتيمة والمكررة.
- إضافة serializer مستقل يحفظ الحالة الفعلية لـ input/textarea/select/contenteditable على نسخة clone من التقرير.
- استخدام serializer في آخر مسار تنفيذي لـ saveReportToArchive.
- استعادة قيم textarea/select المؤرشفة عند فتح التقرير القديم عبر loadReport حيث ينطبق.
- الحفاظ على ربط تقرير الأداء بالمجال المختار واسم مجلد المجال ورسالة نجاح الحفظ والتحقق السحابي الموجودة في النسخة الحالية.

## فحص السلامة
- node --check لـ evidence-library-source.js: ناجح.
- فحص 23 كتلة JavaScript مضمنة داخل agent.html: 0 أخطاء نحوية.
- AGENT_ARCHIVE_FORM_STATE_SERIALIZER_V3: تعريف واحد.
- cleanupInjectedButtons: تعريف واحد.
- لم يتم تعديل ملفات المدير أو المعلم أو الموجه أو بقية الأدوار.

## ملاحظة بنيوية
يحتوي agent.html أصلًا على عدة طبقات تاريخية تعيد تعريف saveReportToArchive. لم تُحذف هذه الطبقات حتى لا تنكسر توافقات الترحيل القديمة، لكن آخر تعريف هو المسار التنفيذي النهائي وقد تم تطبيق التصحيح عليه مباشرة.
