# مراجعة وتصحيح دورة الأعمال الأسبوعية بين الوكيل والمعلم — RL168

## نتيجة الفحص الفعلي

تم فحص الواجهتين، محرك الحالة السحابية، الدالة المنشورة `platform-state`، والبيانات الفعلية داخل `platform_module_state` دون تعديل أو حذف السجلات القائمة.

- الخطط الأسبوعية المنشورة: 107.
- تسليمات المعلمين الموجودة سحابيًا: 17.
- عناصر الأعمال داخل التسليمات: 68.
- العناصر التي تحمل شاهدًا: 47، منها 46 مرتبطة بملفات سحابية.
- قرارات المراجعة المسجلة قبل التصحيح: صفر؛ كانت جميع العناصر بانتظار المراجعة.

## الأسباب المؤكدة

1. واجهة المعلم لم تكن تقرأ جميع مصادر `school_id` المعتمدة في جلسة المنصة، وقد ترفض دمج الخطة المنشورة رغم وصولها سحابيًا.
2. صندوق الوكيل كان يبني العرض انطلاقًا من قائمة المعلمين المحلية فقط؛ لذلك يسقط التسليم السحابي من العرض إذا تأخرت القائمة أو اختلف معرّفها أو لم يعد الحساب ظاهرًا فيها.
3. التسليم المرتبط بأسبوع غير موجود في الذاكرة الحالية للوكيل كان يُتجاهل بدل استعادته من بيانات التسليم.
4. عداد المراجعة كان يحسب الأسبوع المفتوح فقط، فلا يكشف الشواهد المنتظرة في أسابيع أخرى.
5. حالة التسليم كانت تحفظ خادميًا وهي `sending` قبل تثبيت وصولها، ولا تحفظ `delivered` في النسخة الخادمة.
6. رابط إشعار قرار الوكيل كان يوجه المعلم إلى صفحة متابعة الوكيل بدل صفحة أعمال المعلم.
7. الواجهتان لم تكونا تحدثان حالة الخطة والتسليم والقرار تلقائيًا أثناء بقائهما مفتوحتين.

## التصحيح المنفذ

- توحيد قراءة المدرسة والمستخدم من `PlatformCloudSession` ومفاتيح المدرسة المعتمدة.
- تثبيت هوية المعلم من الخطة المنشورة قبل إنشاء التسليم.
- إظهار كل تسليم سحابي للوكيل حتى إن لم يظهر صاحبه مؤقتًا في قائمة المعلمين.
- استعادة الأسبوع من بيانات التسليم دون حذف أو استبدال أي أسبوع محفوظ.
- عداد شامل للشواهد المنتظرة في جميع الأسابيع مع بيان عدد الأسبوع الحالي.
- تحديث تلقائي كل 30 ثانية وعند العودة إلى التبويب في واجهتي المعلم والوكيل.
- حفظ `delivery_status=delivered` ووقت الوصول داخل الدالة الخلفية.
- توجيه إشعار الاعتماد أو الإعادة أو الرفض إلى `teacher_weekly_tasks.html`.

## دورة العمل بعد التصحيح

1. الوكيل يحدد الأسبوع ويضغط «تأكيد وبدء تنفيذ الأسبوع».
2. تنشر الخطة لكل معلم/معلمة داخل المدرسة نفسها مع هوية المسؤول الفعلي.
3. تظهر الأعمال للمعلم، ويرفع الشاهد ويحفظه ثم يضغط «إرسال الأعمال للمسؤول».
4. يظهر التسليم تلقائيًا في تبويب «مراجعة التسليمات» عند الوكيل ويزيد العداد.
5. لا يتخذ الوكيل القرار قبل فتح الشاهد، والإعادة أو عدم الاعتماد تتطلب سببًا.
6. يظهر القرار تلقائيًا للمعلم، ويرتبط إشعاره بصفحة أعمال المعلم.
7. عند إغلاق الأسبوع يمنع الخادم والواجهة أي رفع أو إرسال جديد، مع بقاء العرض والأرشيف.

## الملفات المرتبطة

- `teacher_weekly_tasks.html`
- `deputy_weekly_teacher_followup.html`
- `supabase/functions/platform-state/index.ts`
- `.github/workflows/deploy-supabase-functions.yml` (موجود ويشمل `supabase/functions/**`، لذلك ينشر التعديل تلقائيًا دون إضافة خطوة جديدة).

## حماية البيانات والعزل

- لا توجد أوامر حذف أو تهيئة في التصحيح.
- تبقى الملفات داخل مسار المدرسة والمستخدم الحاليين.
- تحقق الخادم مستمر من `school_id` و`teacher_id` و`reviewer_id`.
- لا يستطيع مسؤول آخر مراجعة تسليم لم ينشر أسبوعه.
